export interface Pagination {
  page: number;
  limit: number;
}

export interface Phone {
  region: string;
  number: string;
}

export interface BirthDate {
  year: number;
  month: number;
  day: number;
}